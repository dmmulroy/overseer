# PROTOTYPE — Kumo Utility theme foundation

Four Kumo theme-token mappings of Overseer's chosen Utility direction, rendered against the same compact issue-tracker specimen.

```bash
npm install
npm run dev
```

Review `/prototype/kumo-utility?variant=A&mode=light`. Use the floating arrows or keyboard `←` / `→` to compare A–D; use **Dark/Light** to inspect both color modes.

Throwaway prototype for #40. Do not promote this code directly into production.
