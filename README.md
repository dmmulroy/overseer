# PROTOTYPE — Utility foundation in Tailwind and shadcn/Base UI

Three application-owned component recipes for Overseer's chosen Utility direction, rendered against the same compact issue-tracker specimen.

```bash
npm install
npm run dev
```

Review `/prototype/utility-foundation?variant=A&mode=light`. Use the floating arrows or keyboard `←` / `→` to compare A–C; use **Dark/Light** to inspect both color modes.

Throwaway prototype for #40. Do not promote this code directly into production.
