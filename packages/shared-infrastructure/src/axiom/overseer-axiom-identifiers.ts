/** Stable Axiom dataset name that retains production traces independently of API deployments. */
export const OVERSEER_PRODUCTION_TRACE_DATASET_NAME = "overseer-production-traces";

/** Identifies the shared Axiom dataset that retains Overseer production traces. */
export const OVERSEER_PRODUCTION_TRACES_DATASET_LOGICAL_ID = "OverseerProductionTracesDataset";

/** Identifies the Axiom API token restricted to Overseer production trace ingestion. */
export const OVERSEER_PRODUCTION_TRACE_INGEST_TOKEN_LOGICAL_ID =
  "OverseerProductionTraceIngestToken";

/** Identifies the Axiom API token restricted to Overseer production trace queries. */
export const OVERSEER_PRODUCTION_TRACE_QUERY_TOKEN_LOGICAL_ID = "OverseerProductionTraceQueryToken";

/** Stable Axiom dataset name shared by every Overseer E2E execution target. */
export const OVERSEER_E2E_TRACE_DATASET_NAME = "overseer-e2e-traces";

/** Identifies the shared Axiom dataset that retains Overseer E2E traces. */
export const OVERSEER_E2E_TRACES_DATASET_LOGICAL_ID = "OverseerE2eTracesDataset";

/** Identifies the Axiom API token restricted to Overseer E2E trace ingestion. */
export const OVERSEER_E2E_TRACE_INGEST_TOKEN_LOGICAL_ID = "OverseerE2eTraceIngestToken";

/** Identifies the Axiom API token restricted to Overseer E2E trace queries. */
export const OVERSEER_E2E_TRACE_QUERY_TOKEN_LOGICAL_ID = "OverseerE2eTraceQueryToken";
