const lines = [
  "",
  "Code added or changed must follow the coding-standards skill and every applicable generic and Effect reference it points to.",
  "- This is an Effect-native project built with Alchemy v2; use idiomatic Effect and Alchemy APIs.",
  "- Avoid async/await and raw Promise workflows unless an external boundary makes them unavoidable; isolate such code in the owning Adapter or localized service implementation that directly owns one concrete client.",
  "- Check repos/effect and repos/alchemy for source and examples before departing from native patterns.",
  "- Before adding a utility, helper, parser, or collection operation, check whether Effect already provides it (for example Array, Record, HashMap, HashSet, Predicate, or Schema); use the existing Effect primitive instead of hand-rolling isRecord/isArray checks or object, Map, and parsing helpers.",
  "- Prefer Effect Option for optional results instead of returning null or undefined.",
  "- Yield existing Clock, Crypto, Config, and application authority services instead of accepting timestamps, renderers, registries, service values, callback operations, or Layers as parameters. Keep request and domain values explicit.",
  "- Name services for stable general capabilities that remain meaningful across callers; prohibit consumer-qualified names, and reserve Repository, Gateway, Provider, Port, and Manager for actual established meanings. Put specific behavior in operation names and compose deeper services from simple capability services.",
  "- Use Effect Schema to encode and decode HTTP, RPC, persistence, and hashed payloads. Avoid direct JSON.stringify() in application source.",
  "- Prefer typed framework APIs and derive contracts from owning types. Avoid raw handlers, duplicated RPC method lists, and casts when Effect or Alchemy can represent the behavior.",
  "- For abstractions, adapters, and seams, use Effect services and layers—not function or constructor injection—and yield dependencies so their requirements propagate through Effects.",
  "- Where a real service exists, use the Effect service module shape:",
  "  export interface Interface { /* smallest cohesive application-owned capability */ }",
  '  export class Service extends Context.Service<Service, Interface>()("...") {}',
  "  export const make = Effect.gen(function* () { /* yield dependencies */ })",
  "  export const layer = Layer.effect(Service, make)",
  "- Add layerTest or layerMemory only when it is reusable and behaviorally honest.",
  "- Before adding an abstraction, check existing owners and apply the deletion test. Yield a concrete client directly when a provider adapter would only forward; extract an adapter for real variation, translation, mechanics, or reuse.",
  "- Parse at outer boundaries and pass parsed domain values inward.",
  "- Make each interface, parameter, function, and combinator earn its place.",
  "- Prefer the simplest correct design and the least code.",
  "",
  "Passing automated checks alone does not prove compliance.",
];

console.log(lines.join("\n"));
